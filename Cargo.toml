[package]
  edition = "2024"
  name    = "packbits"
  version = "0.1.0"

[lib]
  proc-macro = true

[dependencies]
  proc-macro2 = "1"
  quote       = "1"
  syn         = { version = "^2", features = ["full", "extra-traits"] }

[dev-dependencies]
trybuild = "1"
